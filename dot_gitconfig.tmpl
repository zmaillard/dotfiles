# This is Git's per-user configuration file.
[user]
	name = Zachary Maillard
	email = {{ .email }}
[pull]
        rebase = true
[core]
        editor = vim
[pager]
        diff = false
        core = false
        branch = false
{{ if eq .chezmoi.hostname "caribou" }}
[url "https://zmaillard:{{ .ghcli }}@github.com"]
	insteadOf = https://github.com
{{ else }}
[init]
        templatedir = /home/zmaillard/.git-template/
{{ end }}
