{{ if (and (not .ephemeral)) -}}

{{ $packages := list
	"direnv"
	"build-essential"
	"iproute2"
	"fzf"
	"git"
	"curl"
    "zlib1g-dev"
    "libb2-dev"
	"zsh"
	"unzip"
	"ripgrep"
	"libssl-dev"
	"automake"
	"autoconf"
    "neovim"
	"wget"
	"make"
	"jq" -}}


{{ if eq .osid "linux-debian" -}}
{{ $rspPackages := list
	"ntpdate"
	"i2c-tools"
	"libssl-dev"
	"zlib1g-dev"
    "ninja-build"
    "gettext"
    "cmake"
	"libbz2-dev"
	"libreadline-dev"
	"libsqlite3-dev"
	"llvm"
	"libncursesw5-dev"
	"xz-utils"
	"tk-dev"
	"libxml2-dev"
	"libxmlsec1-dev"
	"libffi-dev"
	"liblzma-dev"
	 -}}

{{   $packages = concat $packages $rspPackages -}}

{{ end -}}


{{ $sudo := "sudo " -}}
{{ if eq .chezmoi.username "root" -}}
{{  $sudo = "" -}}
{{ end -}}
{{ else -}}

{{/* Ephemeral - Basic Packages */}}

{{ $packages := list
	"direnv"
	"fzf"
	"git"
	"curl"
	"zsh"
	"ripgrep"
    "neovim"
	"jq" -}}


{{ end -}}
#! /bin/bash

set -eufo pipefail

{{ $sudo }}apt-get update
{{ $sudo }}apt-get install -y {{ $packages | join " " }}
